---
order: 8.5
week: 4
title: "Activity — Telling a Story with ggplot"
subtitle: "Redesign a technically-correct plot into one that communicates"
author: "Bill Perry"
date: today

type: activity
description: >
  Apply the five-second test and the redesign checklist (redundancy,
  legends, ordering, color, titles, annotation) to our leaf data, then
  redesign a second plot on your own.
categories: [ggplot2, data-storytelling]

format:
  html: default
  docx: default
execute:
  echo: true
  warning: false
  message: false
---

# Redesigning a Plot That Says Nothing

### Recap from the lecture

- Start from a finished sentence: "This figure shows that \_\_\_\_."
- The **five-second test** — does a stranger repeat your finding back?
- Cut redundancy (repeated legends/facets), drop unneeded legends, order categories with intent
- Color with intent and colorblind safety in mind
- Title = finding, subtitle = method, caption = fine print
- `annotate()` to point at exactly one thing

> # **How to use this worksheet**
>
> Work through Parts 1–7 in order, typing every line yourself. Part 8 asks you to redesign a second plot from scratch with no scaffolding — that is the real test of whether today's ideas stuck.

------------------------------------------------------------------------

# Part 1 · Load data and look at the "busy" plot

**▶ Run this:**

``` r
library(readxl)
library(tidyverse)
library(janitor)

tree_df <- read_excel("data/2026_06_25_tree_experiment_raw_data.xlsx") %>%
  clean_names()

tree_busy_plot <- tree_df %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot() +
  geom_jitter(width = 0.3, size = 1) +
  facet_wrap(~ side) +
  labs(title = "weight_g by side", x = "side", y = "weight_g") +
  theme_gray()

tree_busy_plot
```

✏️ **Your turn — the five-second test:** Cover the plot, show it to a classmate for five seconds, then hide it. Ask them what it showed. Write down exactly what they said.

```
What my classmate said the plot showed:
Did they repeat back "shady leaves are heavier"?  Y / N
```

✏️ **Your turn:** List at least three specific things wrong with this plot, using the lecture's vocabulary (redundancy, legend, title, order, color).

```
Problem 1:
Problem 2:
Problem 3:
```

------------------------------------------------------------------------

# Part 2 · Fix \#1 — cut the redundant facet

**▶ Run this:**

``` r
tree_plot_v2 <- tree_df %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot(alpha = 0.6, outlier.shape = NA) +
  geom_jitter(width = 0.15, alpha = 0.6, size = 2) +
  theme_minimal()

tree_plot_v2
```

✏️ **Your turn:** Why was `facet_wrap(~ side)` redundant here, specifically? (What other part of the plot already showed the same grouping?)

```
Your answer:
```

------------------------------------------------------------------------

# Part 3 · Fix \#2 — drop the legend

**▶ Run this:**

``` r
tree_plot_v3 <- tree_df %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot(alpha = 0.6, outlier.shape = NA) +
  geom_jitter(width = 0.15, alpha = 0.6, size = 2) +
  theme_minimal() +
  theme(legend.position = "none")

tree_plot_v3
```

✏️ **Your turn:** Did removing the legend lose any information a reader needs? Why or why not?

```
Your answer:
```

------------------------------------------------------------------------

# Part 4 · Fix \#3 — order with intent

**▶ Run this:**

> 🔮 **Predict first:** Which side will plot on the left after `fct_relevel(side, "sunny", "shady")` — the same order as before, or reversed?

``` r
tree_plot_v4 <- tree_df %>%
  mutate(side = fct_relevel(side, "sunny", "shady")) %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot(alpha = 0.6, outlier.shape = NA) +
  geom_jitter(width = 0.15, alpha = 0.6, size = 2) +
  theme_minimal() +
  theme(legend.position = "none")

tree_plot_v4
```

✏️ **Your turn:** Was the default (alphabetical) order the same as the `fct_relevel()` order here? If your dataset had three groups instead of two, why would deliberate ordering matter even more?

```
Same order?  Y / N
Why ordering matters more with 3+ groups:
```

------------------------------------------------------------------------

# Part 5 · Fix \#4 — color with intent

**▶ Run this:**

``` r
tree_plot_v5 <- tree_df %>%
  mutate(side = fct_relevel(side, "sunny", "shady")) %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot(alpha = 0.7, outlier.shape = NA) +
  geom_jitter(width = 0.15, alpha = 0.6, size = 2) +
  scale_fill_manual(values = c(sunny = "#E69F00", shady = "#0072B2")) +
  theme_minimal() +
  theme(legend.position = "none")

tree_plot_v5
```

✏️ **Your turn:** Why are `#E69F00` and `#0072B2` a safer choice than, say, plain red and green?

```
Your answer:
```

------------------------------------------------------------------------

# Part 6 · Fix \#5 — a title that states the finding

**▶ Run this:**

``` r
tree_final_plot <- tree_df %>%
  mutate(side = fct_relevel(side, "sunny", "shady")) %>%
  ggplot(aes(x = side, y = weight_g, fill = side)) +
  geom_boxplot(alpha = 0.7, outlier.shape = NA) +
  geom_jitter(width = 0.15, alpha = 0.6, size = 2) +
  scale_fill_manual(values = c(sunny = "#E69F00", shady = "#0072B2")) +
  labs(
    title = "Shady-side leaves are heavier than sunny-side leaves",
    subtitle = "Each point is one leaf; boxes show the median and interquartile range",
    x = NULL,
    y = "Leaf weight (g)",
    caption = "n = 10 leaves per side"
  ) +
  theme_minimal() +
  theme(legend.position = "none", plot.title = element_text(face = "bold"))

tree_final_plot
```

✏️ **Your turn:** Rewrite the title as if the data had shown *no difference* between sides. Does a title always have to declare a dramatic finding?

```
Your rewritten title:
Your answer:
```

------------------------------------------------------------------------

# Part 7 · Fix \#6 — `annotate()` to point at the finding

**▶ Run this:**

``` r
mean_shady <- tree_df %>% filter(side == "shady") %>%
  summarize(m = mean(weight_g, na.rm = TRUE)) %>% pull(m)

tree_final_plot +
  annotate(
    "text", x = 2.35, y = mean_shady,
    label = paste0("Shady mean:\n", round(mean_shady, 1), " g"),
    size = 3.2, color = "#0072B2", hjust = 0
  )
```

✏️ **Your turn:** Try moving the annotation's `x` and `y` values so the label doesn't overlap any boxplot elements. Record the values that worked.

``` r
# Write your adjusted code here:
```

```
Final x, y values that worked:
```

------------------------------------------------------------------------

# Part 8 · Redesign a second plot — no scaffolding

Repeat the whole process on `height_cm` **or** `width_cm` instead of `weight_g`. Do not copy the code above line-by-line — write it from memory, checking back only if you get stuck.

✏️ **Your turn:** Before writing any code, finish this sentence:

```
"This figure shows that ____________________________________."
```

**▶ Build your final figure:**

``` r
# Write your full redesigned plot here, using every fix from today:
```

✏️ **Your turn — run the five-second test again**, on your own plot, with a different classmate.

```
What they said the plot showed:
Did it match your sentence?  Y / N
If not, what will you change:
```

------------------------------------------------------------------------

# Part 9 · Review and checkpoint

At this point you should be able to:

- [ ] Write a one-sentence finding *before* building a final figure
- [ ] Apply the five-second test to judge whether a plot communicates
- [ ] Identify and remove redundant plot elements (facets, legends, titles)
- [ ] Use `fct_relevel()` to order categories with intent
- [ ] Choose a colorblind-safe palette with `scale_fill_manual()`
- [ ] Write a title that states the finding, with subtitle and caption used correctly
- [ ] Use `annotate()` to highlight exactly one thing on a plot
- [ ] Redesign a plot on a different variable with no scaffolding

------------------------------------------------------------------------

# Getting unstuck

1.  **Legend won't disappear?** Check you used `theme(legend.position = "none")`, not `theme(legend.position = NULL)`.
2.  **`fct_relevel()` error?** The column must already be a factor or character — check with `class(tree_df$side)`.
3.  **Colors look wrong?** `scale_fill_manual()` names must exactly match the values in your data — check with `unique(tree_df$side)`.
4.  **Annotation label is cut off or overlapping?** Adjust the `x`/`y` coordinates — they are in the same units as your axes, not fixed screen positions.
5.  **Bring the exact error** (copy-paste it) to class, Canvas, or office hours.

> 💡 **Key idea:** A finding-based title is the cheapest, highest-impact edit you can make to any figure. If you do nothing else from today's lecture, do that one.

------------------------------------------------------------------------

*End of this activity. Next: apply every fix here to your own final-project take-home figure.*
